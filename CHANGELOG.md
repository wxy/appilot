# Changelog

## [1.3.0] - 2026-09-26 — npm 包与 macOS DMG

### 新增与改进
- **发布工作台**：按平台隔离商店文案和 Keynote 截图套件；为已定稿文案建立可追溯修订；统一多平台发布事实与就绪状态。
- **上架后推广**：新增独立的 X 系列推广流程，支持按商店平台生成、编辑和确认文案及最终配图。
- **排名**：关键词本地化建议、采集预算治理、多词对比与排名/竞品数据展示改进。

### 修复与可靠性
- 守护进程启动以可连接的控制 socket 判定健康，强化权限、仲裁及进程退出处理。
- 收紧 AI 密钥、ASC 私钥、截图预览与远程请求的主进程安全边界。
- 修复发布变更范围和 PR 排序、iTunes 请求超时、流式 AI 重试状态、截图移除、日期比较和快照清理事务。
- 完善测试发现、lint 和发布前 CI 门禁；MCP 初始化返回与包版本一致的 serverInfo.version。

### 发布范围
- 九个公开的 `@appilot-labs/*` npm 包统一升至 1.3.0，内部依赖范围同步为 `^1.3.0`。
- 同时提供 arm64 与 x64 macOS DMG；安装包在发布前分别完成 Developer ID 签名、Apple 公证、贴票与 Gatekeeper 验证。
- 共享 SQLite schema 从 15 升至 16，增加 lease 的 `leaderPid` 字段；打开存储时自动迁移。

## [0.4.4] - 2026-08-31 — 里程碑版本

自 0.3.0 以来的完整周期（0.4.1 数据层 → 0.4.2 UI → 0.4.3 洞察 → 0.4.4 发布生命周期）。

### 新增
- **发布工作台**：单工作目标重构、流程状态栏（GitHub release → 商店版本）、语言标签页、
  草稿材料保留、从公告创建/从草稿重建、store 对齐徽标、发布清单（权限/能力审计、
  分支语言检测、截图位置、AI 流式字符）。
- **发布来源**：GitHub releases 作为主发布源（git tag 降级）、ASC 当前版本查询 +
  商店查询降级、copy draft 按 appVersion 身份与迁移、目标版本预填。
- **历史查看器**：历史标签、语言标记、一键切换、最新文案信息。
- **评论洞察**：反馈收件箱、AI 主题聚类、总览反馈主题卡、竞品差异摘要、评论/趋势页。
- **数据层**：ASC 只读客户端（JWT）、GitHub 流量与 release 资产下载采集、
  App Store 评论 RSS 采集器、确定性 release readiness 检查、build 状态映射。
- **关键词/竞品**：per-platform 竞品、竞品排名种子与采样、Ruby 标注、语言级关键词、
  copy-gap 适配、批量翻译、矩阵排名分布图。
- **调度**：加速模式（积压快速清空）、任务中心立即执行按钮、任务中心重构与筛选、
  数据变更推送通道（自动刷新 + 更新提示）。
- **总览**：项目活动卡片（提交热力图 + release 标记）、排名分布图替代趋势线、
  关键词 Ruby chip 样式。

### 质量
- 测试覆盖 51 个测试文件（约 400+ 断言），typecheck 与生产构建通过。
- 并发写安全：调度任务与 IPC 读改写冲突修复、调度合并只回写实际执行的任务、
  立即执行与定时执行去重。
- 商店文案生成与修订合并为单一代码路径（`baseLocalization` 参数化）。

### 已知边界
- ASC 凭据写入动作（版本创建/提交）留待后续阶段。
- ASC 商店表现（Analytics Reports）因 Admin 权限过重而搁置（见
  `docs/research/asc-store-performance-api.md`）。
- 当前里程碑只发布 macOS；Windows 安装包留待后续。

## [0.3.0] - 2026-08-23 — 里程碑版本

首个可分发的里程碑版本（macOS DMG / Windows 安装包）。

### 新增
- 项目接入与本地仓库只读分析（平台/语言/商店链接自动识别）。
- 总览页：副驾驶简报、排名趋势图、Top 关键词、发布状态卡片。
- 发布工作台：基于 git tag/提交的发布检测、变更摘要确认、AI 生成 App Store
  文案、多语言翻译、历史文案列表。
- GitHub 集成：release 公告与 PR 素材（公开仓库匿名可用，Token 支持私有与草案）。
- 关键词：AI 生成与整理、语言 × 平台 × 商店交叉跟踪矩阵、自动排名采集调度。
- 任务中心：调度时间线、执行统计、每项目 GitHub 同步任务。
- 设置：AI 提供方配置、全局/项目级 GitHub 与 App Store Connect 凭据。

### 质量
- 并发写安全（调度任务与 IPC 的读改写冲突修复）、GitHub 请求缓存与超时处理。
- 测试覆盖：引擎层 + 纯逻辑模块（约 200 个断言）。
- 代码拆分重构：渲染端 App.tsx 6342 → 327 行，主进程 ipc.ts 2581 → 14 行。

### 已知边界（规划中）
- 评论洞察、长期趋势页为占位。
- App Store Connect 凭据的写入动作（版本创建/提交）留待后续阶段。
