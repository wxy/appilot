# AppPost 实施方案

> ⚠️ **已过时，待重写。** 此方案基于 2026-07-14 第二次修订的设计文档编写，此后设计经历了重大变更：
> - 移除 Product Hunt
> - AI 前移至 Phase 0（Phase 1 不再含 AI 基础设施任务）
> - 新增 Repo Analyzer / Project Registry / 多仓库 / URL 回填 / README 完善建议 / AI 用量追踪 / 全面日志
> - 推广阶段新增 teaser/preLaunch / Release 监控取代 commit 触发
> - Phase 5 新增效果反馈闭环和竞品分析
>
> **在设计文档拆分确认稳定后，需基于 [拆分后的设计文档集](../README.md) 完全重写本方案。** 当前文件仅保留结构参考，不应用于执行。
>
> 状态：⚠️ 已过时 | 日期：2026-07-14（原始） | 标记过时：2026-07-14

---

## 0. 执行前准备（Day 0，非工程任务，建议立即启动，不阻塞后续编码）

这些事项耗时不可控（依赖第三方审批），必须最早启动，绝不能等到对应 Phase 才开始：

| # | 事项 | 为什么现在做 | 负责 |
|---|------|--------------|------|
| 1 | 发送 Product Hunt 商用授权确认邮件（对应设计文档 P0-1） | 官方审批周期未知，越晚发越可能拖慢 Phase 3；不阻塞 Phase 0~2 开发 | 你 |
| 2 | 注册 Twitter/X Developer Portal 应用 + 购买/预估首批 credit 额度 | 确认真实价格和额度到账周期后才能定 Phase 1 的预算熔断阈值 | 你 |
| 3 | 注册 Reddit App（获取 client_id，选择"installed app"只读类型） | 免费，5 分钟完成，Phase 2 直接可用 | 你 |
| 4 | 创建 Google Cloud 项目并启用 YouTube Data API v3（用于本机自用测试） | 免费，Phase 0/1 的"用户自助 Key 向导"需要先跑通一次自己的流程才能写文档 | 你 |
| 5 | 创建 Discord Application + Bot（先不申请 Message Content Intent，<100 服务器自用默认可用） | 免费，Phase 2 直接可用 | 你 |
| 6 | 建 GitHub 仓库结构 + Projects 看板，按 Phase 建 Milestone，本文档任务逐条建 Issue | 便于跟踪，避免长周期项目"设计文档写完就没人跟踪进度" | 你 |
| 7 | 落地第 18.3 节两个待决策事项（见下） | 避免执行中反复纠结 | 你 |

**待决策事项的最终决定（本方案采用）：**
- **崩溃上报（P1-4）**：并入 **Phase 1**，与 CI 基线一起做（复用同一批 CI/构建基础设施投入，边际成本低）。
- **多账号管理（P1-7）**：**MVP 阶段维持排除**，理由——验证阶段单账号足以满足"自用"目标，多账号留到 Phase 4（插件生态阶段）根据实际反馈评估是否值得投入；`accounts` 表已是多行设计，届时只需补 UI/Engine 层，不涉及 Schema 变更。

---

## 1. 总体路线图与工作量估算

| Phase | 内容 | 预估工作日 | 累计工作日 | 关键产出（演示标准） |
|-------|------|-----------|-----------|----------------------|
| Phase 0 | 零依赖手动模式 | 22 | 22 | 无需任何平台账号，可完成"编辑→（Web Intent/手动）发布→手动填报统计→查看趋势图"全流程 |
| Phase 1 | Twitter 全自动 + 基础设施完备 | 29 | 51 | 真实 Twitter 账号可自动发布 + 拉取回复 + AI 辅助生成文案；CI/签名/自动更新/崩溃上报全部跑通 |
| Phase 2 | Reddit + Discord | 17 | 68 | 三平台（Twitter/Reddit/Discord）可在 Composer 中同时勾选发布，Inbox 聚合三平台互动 |
| Phase 3 | Product Hunt + YouTube | 15.5 | 83.5 | 五平台全部接入（PH/YouTube 视具体授权情况可能仍是 Tier 0/1），视频上传与配额监控可用 |
| Phase 4 | 统计与插件生态 | 10.5 | 94 | 跨平台统计对比图可用；社区插件目录可被动态加载 |
| Phase 5 | AI 增强模式 | 12.5 | 106.5 | 半自动规则回复、全自动代理模式（含频控/敏感词/人工回退）可用 |

**总计约 106.5 个工作日 ≈ 21 周 ≈ 5 个月**（全职 solo，不含 buffer）。建议整体预留 **15~20% buffer**（约 16~21 天），主要用于吸收：Windows 环境验证反复（OAuth loopback、代码签名）、平台 API 审批等待、Flutter 桌面端小众问题排查。

---

## 2. 通用执行规范

- **分支策略**：`main` 为稳定分支；每个 Phase 开一个 `phase/N-xxx` 分支，Phase 验收通过后合并回 `main` 并打 tag（如 `v0.1.0` = Phase 0 完成）。
- **Definition of Done（每个 Phase 共同标准）**：
  1. 该 Phase 列出的所有任务对应 Issue 全部关闭；
  2. 能跑通"关键产出"里描述的演示场景（建议录屏留存，作为里程碑记录）；
  3. 新增/修改的 Engine、Plugin 代码有单元测试覆盖（Phase 1 起强制，Phase 0 可放宽）；
  4. `docs/` 下设计文档如有偏离实现的地方，同步更新（避免文档与代码逐渐脱节）。
- **任务粒度**：本文档拆到"1 人 0.5~3 天可完成"的粒度，超过 3 天的任务在执行时应再拆子任务。
- **每日节奏建议**：全职 solo 项目容易因为"设计→写码→卡在某个平台 API 细节"而失焦，建议每天开始前用 5 分钟对照本文档的 Issue 列表选定当日任务，收工前更新 Issue 状态。

---

## 3. Phase 0 任务拆解：零依赖手动模式（22 天）

> 目标：不依赖任何平台账号，跑通"编辑草稿 → 发布（Web Intent/手动清单）→ 手动填报统计 → 趋势图 → 到期提醒"完整闭环，同时把 Phase 1+ 都要复用的基础设施和 Schema 一次性打好。

| # | 任务 | 估时 | 依赖 | 验收标准 |
|---|------|------|------|----------|
| 0.1 | Monorepo 结构搭建（`app/` `engine/` `plugins/plugin_interface/` + melos 或纯 path 依赖） | 1d | - | `flutter run` 能跑起一个空白窗口，`engine` 包被 `app` 正确引用 |
| 0.2 | drift Schema 设计与初版建表（accounts / posts / interactions / content_templates / post_analytics / reminders） | 2d | 0.1 | 所有表字段与设计文档 7.1 一致，drift 生成代码无报错 |
| 0.3 | Migration 框架搭建 + 首个 migration 测试 | 0.5d | 0.2 | 能模拟"升级到下一版本 Schema"并跑通迁移测试 |
| 0.4 | i18n 框架（flutter_localizations + ARB，中英双语骨架） | 1d | 0.1 | 切换系统语言，UI 文案随之切换（哪怕只有 2~3 条文案） |
| 0.5 | 主题系统（明暗色） | 1d | 0.1 | 系统深色模式切换时应用跟随切换 |
| 0.6 | 错误处理框架（PlatformException/EngineException 分层 + 日志脱敏工具） | 1d | 0.1 | 故意抛一个异常，验证日志文件中 Token 类字段被脱敏 |
| 0.7 | 系统托盘 + 开机自启动 | 1d | 0.1 | 关闭主窗口后应用仍在托盘运行，重启电脑后自动启动 |
| 0.8 | Riverpod 状态管理 + GoRouter 路由骨架（Composer/Inbox/Analytics/Settings 四个空页面可跳转） | 1d | 0.1 | 左侧导航四个入口都能跳转到对应空页面 |
| 0.9 | Composer 编辑器 UI（标题/正文/附件占位 + 本地草稿自动保存 30s） | 3d | 0.2, 0.8 | 输入内容后杀掉进程重开，草稿还在 |
| 0.10 | 内容模板功能（保存/复用文案，`content_templates` 表落地） | 1d | 0.9 | 保存一条模板，能在新草稿中一键套用 |
| 0.11 | `PlatformPlugin` 接口骨架 + `PublishMode`/`TrackingMode` 枚举定义（先不接入真实平台，用 Mock Plugin 验证） | 1.5d | 0.1 | 一个 Mock Plugin 能被 Plugin Registry 加载并声明能力 |
| 0.12 | Web Intent 构建与跳转（Twitter `intent/tweet`、Reddit 预填 submit 链接） | 2d | 0.9, 0.11 | 点击按钮后系统浏览器打开且文案已预填 |
| 0.13 | 手动发布清单 UI（Discord/PH/YouTube 走"复制文案 + 打开官网"路径 + 打勾回填 permalink） | 2d | 0.9, 0.11 | 五个平台在清单里都能走完"标记已发布"流程，`posts.publish_mode` 正确写入 |
| 0.14 | `AnalyticsEngine` 落地（`recordApiStats` 先留空实现，`submitManualStats`/`watchTrend` 完整实现） | 1.5d | 0.2 | 手动提交一条统计后，`post_analytics.source = manual` 且能查询到 |
| 0.15 | 手动统计登记表单 UI（浏览量/点赞/评论数 + 备注） | 1.5d | 0.13, 0.14 | 表单提交后立即在 Analytics 页看到新数据点 |
| 0.16 | Reminders 到期提醒（`TaskScheduler.reminderCheck` + 系统通知） | 1.5d | 0.2, 0.13 | 手动设置一条 3 秒后到期的提醒，能收到系统通知并点击跳转到填报表单 |
| 0.17 | Analytics 仪表盘最小版（单帖子趋势折线图，用手动数据） | 1.5d | 0.14 | 连续填报 3 次统计后，图表显示 3 个数据点的折线 |
| 0.18 | Phase 0 整体验收（端到端手动测试 + 录屏存档） | 1d | 上述全部 | 演示"编辑→发布清单→手动填报→趋势图→提醒"全流程无崩溃 |

小计：**22 天**

---

## 4. Phase 1 任务拆解：Twitter 全自动 + 基础设施完备（29 天）

> 目标：接入第一个真实付费平台（Twitter/X），验证 OAuth、Credential Vault、AI 辅助、CI/CD、签名与自动更新这套"重基础设施"能跑通，为后续插件复用铺路。

| # | 任务 | 估时 | 依赖 | 验收标准 |
|---|------|------|------|----------|
| 1.1 | `Credential` 抽象类 + `OAuthToken`/`ApiKey` 子类实现 | 1d | Phase 0 | 能序列化/反序列化并写入 Keychain |
| 1.2 | Credential Vault 封装（macOS Keychain，Windows Credential Manager 可后补） | 2d | 1.1 | 存入一个 Token，重启应用后仍能读出且未落盘明文 |
| 1.3 | `OAuthCallbackServer`（本地 loopback HTTP server 通用实现） | 2d | 1.2 | 用任意 OAuth 测试应用能收到授权回调并解析出 code |
| 1.4 | Windows 环境验证 loopback 是否被防火墙拦截，若不可行切换为自定义 URL Scheme | 1d（验证）+ 1d（备选方案，风险预留） | 1.3 | 在 Windows 机器/VM 上跑通一次完整 OAuth 回调 |
| 1.5 | Twitter Plugin：OAuth 授权流程接入 | 2d | 1.3 | 点击"连接 Twitter"后完成授权，`accounts` 表新增一条记录 |
| 1.6 | Twitter Plugin：`publish()` 实现（文本+图片） | 2d | 1.5 | 从 Composer 发布一条真实推文，`PostResult.success = true` |
| 1.7 | Twitter Plugin：`fetchNewInteractions`/`getStats` 实现（先用最小预算验证可行性） | 2d | 1.5 | 能拉到自己刚发的推文的回复列表 |
| 1.8 | 用量预算熔断（Task Scheduler 监控月度花费 + 超限自动降频/暂停 + 设置页展示剩余额度） | 2d | 1.7 | 手动把预算上限设为 0，验证轮询自动暂停并有提示 |
| 1.9 | Composer 接入真实 Twitter 发布（从 Phase 0 的 manualOnly UI 切换为 apiAuto 按钮） | 1.5d | 1.6, 0.9 | 已连接账号时 Composer 显示"自动发布"而非"预填跳转" |
| 1.10 | Inbox 接入真实 Twitter 互动数据 | 2d | 1.7 | Inbox 列表显示真实回复，标记已读/回复状态可用 |
| 1.11 | `AIProvider` 接口 + OpenAI 兼容 Provider 实现 | 1.5d | Phase 0 | 用一个真实 API Key 跑通一次 `chat()` 调用 |
| 1.12 | AI 配置 UI（产品描述/目标受众/品牌语气 + **AI 数据出境提示**，对应 P0-4） | 1.5d | 1.11 | 首次开启 AI 功能时弹出数据出境说明，用户需显式勾选同意 |
| 1.13 | AI 辅助模式：内容生成草稿 | 2d | 1.11, 0.9 | 点击"AI 生成"后 Composer 正文被填充合理文案 |
| 1.14 | AI 辅助模式：回复建议 | 1.5d | 1.11, 1.10 | 在 Inbox 选中一条互动，能获得 AI 回复建议草稿 |
| 1.15 | CI 基线（GitHub Actions：lint + test + build，覆盖 macOS，Windows 可后补） | 1.5d | Phase 0 | PR 提交后自动跑 lint/test，失败则阻止合并 |
| 1.16 | 单元测试/集成测试基线（Mock Plugin 用于 Composer/Inbox 集成测试） | 2d | 1.15 | 覆盖率报告可生成，关键路径（发布/拉取互动）有测试 |
| 1.17 | macOS Developer ID 签名 + 公证 | 1.5d | 1.15 | 构建产物在全新 Mac 上双击可直接打开，无 Gatekeeper 警告 |
| 1.18 | 自动更新框架接入 | 2d | 1.17 | 发布一个新版本后，旧版本能检测到并提示/完成更新 |
| 1.19 | 崩溃上报 opt-in 接入（承接 Day 0 决策：并入本 Phase） | 1d | 1.15 | 故意触发一次崩溃，设置开启上报时能在后台看到上报记录，关闭时不上报 |
| 1.20 | Phase 1 整体验收（端到端演示 + 录屏存档） | 1d | 上述全部 | 演示"AI 生成文案→Twitter 自动发布→Inbox 收到真实回复→AI 建议回复"全流程 |

小计：**29 天**

---

## 5. Phase 2 任务拆解：Reddit + Discord（17 天）

> 目标：验证"多插件并存"的 Post Queue/Inbox 设计，同时落地 Reddit 合规清理这类平台特有的强制义务。

| # | 任务 | 估时 | 依赖 | 验收标准 |
|---|------|------|------|----------|
| 2.1 | Reddit Plugin：只读 OAuth 授权（installed app 类型） | 1.5d | Phase 1 (Credential Vault/OAuthCallbackServer 复用) | 授权后能拉到自己账号的基本信息 |
| 2.2 | Reddit Plugin：`publish()` 实现（自动发布） + Web Intent fallback（未授权时走预填提交页） | 1.5d | 2.1, 0.12 | 已授权走自动发布，未授权走预填跳转，两条路径都能验证 |
| 2.3 | Reddit Plugin：互动拉取（分数/评论数/评论正文，免费只读 API） | 1.5d | 2.1 | Inbox 能看到 Reddit 帖子下的真实评论 |
| 2.4 | Reddit 合规清理任务（定期比对远端删除状态，48h 内级联清除本地缓存，对应 Data API Terms 强制要求） | 2d | 2.3 | 模拟远端删除一条评论，清理任务运行后本地对应数据被删除 |
| 2.5 | Discord Bot 注册与接入文档整理（面向用户的设置向导文案） | 1d | Day 0.5 | 一份可交给测试用户看懂的"如何创建 Discord Bot"引导文档 |
| 2.6 | Discord Plugin：发布（Bot/Webhook 消息） | 1.5d | 2.5 | 从 Composer 能把消息发到指定 Discord 频道 |
| 2.7 | Discord Plugin：互动拉取（reaction/回复，注意 Message Content Intent 限制） | 2d | 2.6 | Bot 能检测到频道内针对该消息的新回复 |
| 2.8 | Composer 多平台选择 UI 扩展（同时勾选 Twitter/Reddit/Discord） | 1.5d | Phase 1, 2.2, 2.6 | 一次编辑内容能同时推送到三个平台，各自走各自的 publishMode |
| 2.9 | Post Queue：多平台并行发布 + 排队管理 + 跨平台防刷屏最小时间间隔 | 2.5d | 2.8 | 同时发布到三平台时互不阻塞；设置最小间隔后连续发布会被节流并提示 |
| 2.10 | Inbox 平台筛选器 | 1d | 2.3, 2.7, 1.10 | 可按平台筛选互动列表 |
| 2.11 | Phase 2 整体验收 | 1d | 上述全部 | 演示"一次编辑，三平台同时发布，Inbox 聚合三平台回复"全流程 |

小计：**17 天**

---

## 6. Phase 3 任务拆解：Product Hunt + YouTube（15.5 天）

> 目标：接入两个"发布能力受限"的平台，验证 Tier 0/1 混合模式的实际体验，并落地配额/授权相关的保护机制。

| # | 任务 | 估时 | 依赖 | 验收标准 |
|---|------|------|------|----------|
| 3.1 | Product Hunt 只读 API 接入（client-only token，投票数/评论数追踪） | 1.5d | Phase 0 (AnalyticsEngine) | 能拉到自己产品页的真实投票数 |
| 3.2 | PH 辅助模式 UI（AI 生成发布文案 + 手动提交引导链接） | 1.5d | 3.1, 1.13 | 点击"生成 PH 文案"后得到可直接使用的文案，并引导打开 producthunt.com |
| 3.3 | PH 商用授权状态检查（若 Day 0 邮件已获批，接入写权限；若未获批，明确锁定为只读+手动模式，UI 提示原因） | 0.5d | Day 0.1 | UI 上有清晰的状态提示，不让用户以为"应该能自动发布却发布失败" |
| 3.4 | YouTube 用户自助 API Key 设置向导（对应 18.2 节的共享配额风险修复：**不用 AppPost 内置 Key**） | 1.5d | Phase 0 | 用户跟着向导在自己的 Google Cloud 账号下拿到 Key 并粘贴进设置页 |
| 3.5 | YouTube 只读追踪（`videos.list`/`commentThreads.list`，views/likes/comments） | 1.5d | 3.4 | 能拉到自己视频的真实播放量/点赞/评论 |
| 3.6 | YouTube OAuth 上传实现（`videos.insert`） | 2d | 1.3（复用 OAuthCallbackServer） | 从应用内直接上传一个测试视频到 YouTube 成功 |
| 3.7 | YouTube 配额监控与预警（默认 10,000 单位/天，`videos.insert` 消耗 1600/次） | 1.5d | 3.6 | 手动模拟接近配额上限，UI 提前给出预警而不是等到失败 |
| 3.8 | Composer 视频上传区域（含上传进度 + 断点续传） | 3d | 3.6, 0.9 | 中断上传后能从断点继续，而不是重新上传整个文件 |
| 3.9 | 跨平台内容模板功能完善（模板可标记适用平台，Composer 按当前选中平台过滤模板） | 1.5d | 0.10, 2.8 | 保存一个仅标记"Twitter+Reddit"的模板，在选中 Discord 时不会被推荐 |
| 3.10 | Phase 3 整体验收 | 1d | 上述全部 | 演示五个平台在 Composer 里都可选，YouTube 上传/PH 手动引导均可用 |

小计：**15.5 天**

---

## 7. Phase 4 任务拆解：统计与插件生态（10.5 天）

> 目标：从"能用"到"好用"，补齐跨平台统计对比和面向社区的插件加载机制。

| # | 任务 | 估时 | 依赖 | 验收标准 |
|---|------|------|------|----------|
| 4.1 | 统计仪表盘增强（跨平台对比柱状图/趋势折线图，区分 `source = api` / `manual`） | 3d | 全部平台插件 | 能看到"过去 30 天各平台浏览量对比"图表 |
| 4.2 | 插件管理界面（安装/启用/禁用/配置 + 能力声明展示，展示每个插件当前的 publishMode/trackingMode） | 2.5d | Phase 1~3 全部插件 | 能在界面上看到"Reddit：Tier1 只读追踪 / Twitter：Tier2 全自动"这类清晰标注 |
| 4.3 | 社区插件加载机制（`~/.apppost/plugins/` 动态加载 + 版本校验） | 3d | Phase 0 (Plugin Registry) | 手写一个符合接口的第三方插件包，放入目录后能被识别加载 |
| 4.4 | 插件加载失败隔离测试（故意放一个损坏的插件包，验证不影响其他插件） | 1d | 4.3 | 损坏插件报错但应用其他功能正常 |
| 4.5 | Phase 4 整体验收 | 1d | 上述全部 | 演示"安装一个自制插件 + 查看跨平台统计对比图" |

小计：**10.5 天**

---

## 8. Phase 5 任务拆解：AI 增强模式（12.5 天）

> 目标：把 AI 从"辅助模式"升级到可配置的半自动/全自动，并补齐安全约束和错误处理的覆盖面。

| # | 任务 | 估时 | 依赖 | 验收标准 |
|---|------|------|------|----------|
| 5.1 | 半自动回复规则引擎（`ReplyRule` 定义 + 匹配执行） | 3d | Phase 1 (AI Engine) | 配置一条规则后，符合条件的新互动能被自动按规则处理，不符合的进入人工待办 |
| 5.2 | 全自动代理模式 + 安全约束（发布/回复频率上限、敏感词过滤、人工随时接管） | 3d | 5.1 | 开启全自动后，超过设定频率上限时自动暂停并通知；命中敏感词时拒绝生成 |
| 5.3 | AI 操作审计日志 UI（`ai_actions` 表可视化查看） | 1.5d | 5.1, 5.2 | 能按时间/类型筛选查看每一次 AI 自主操作的输入输出 |
| 5.4 | AI 发布策略建议（最佳发帖时机） | 2d | Phase 4 (统计数据) | 基于历史统计给出"建议本周三 20:00 发布"之类的具体建议 |
| 5.5 | 全面错误处理覆盖（补齐设计文档 9.2 节列出的全部场景的测试用例） | 2d | 全部 Phase | 逐条场景（Token 过期/限频/断网/部分成功/插件失败/数据库损坏）都有对应测试并通过 |
| 5.6 | Phase 5 / MVP 整体验收 | 1d | 上述全部 | 完整走一遍从 Phase 0 到 Phase 5 的核心场景，作为 MVP 交付的最终演示 |

小计：**12.5 天**

---

## 9. 风险登记表（执行期需持续跟踪）

| 风险 | 触发信号 | 应对 |
|------|----------|------|
| Twitter/X 实际付费成本超预期 | 1.8 用量熔断频繁触发 | 降低轮询频率、减少自动拉取范围，评估是否值得付费升级额度 |
| Product Hunt 商用授权迟迟未获批 | Day 0 邮件超过 4 周无回复 | Phase 3 时 3.3 任务直接锁定为"只读 + 手动模式"长期方案，不再等待 |
| Windows OAuth loopback 被防火墙拦截 | 1.4 验证失败 | 切换到自定义 URL Scheme 方案（已在设计文档 P0-3 中预留备选） |
| YouTube 配额在追踪场景下也可能不够（多帖子频繁轮询） | 3.7 监控发现日消耗持续接近上限 | 降低轮询频率，优先用 `commentThreads.list`（1 单位）而非重复调用高消耗接口 |
| 全职 solo 进度落后预期 | 单个 Phase 实际耗时超估算 30%+ | 优先保证 Phase 0~2（核心闭环+多平台），Phase 4~5 可视情况延后或简化 |

---

## 10. 与设计文档的关系

- 本文档是设计文档第 13 节"构建计划"的任务级展开，两者应保持同步：若实施中发现某个 Phase 的范围需要调整，应同时回写设计文档第 13 节。
- 本文档第 0 节的两项决策（崩溃上报归入 Phase 1、多账号维持排除）用于闭合设计文档第 18.3 节留下的"待决策事项"，建议同步在设计文档 2.3 节补一句排除理由，保持文档内部一致。
